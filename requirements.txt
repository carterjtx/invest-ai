# Invest AI Assistant - Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (required)
# tkinter is included with Python, no need to install separately

# Price data - Yahoo Finance API wrapper
yfinance>=0.2.28

# HTTP requests for API calls
requests>=2.31.0

# AI Assistant - Anthropic Claude API
anthropic>=0.18.0

# Charts and visualizations
matplotlib>=3.7.0

# Note: The following are optional but recommended for full functionality
# newsapi-python>=0.2.7  # Alternative NewsAPI client (we use requests directly)
